# v0.6 AI-Native Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add YAML-backed parser/rule CRUD, Claude API generation, and an MCP server to TinySIEM.

**Architecture:** Parser and rule YAML files live on disk in `app/decoder/decoders/` and `app/rules/rules/`; custom user files go in `custom/` subdirectories. FastAPI routers expose CRUD + generate endpoints. An `app/ai/claude.py` module wraps the Anthropic SDK. An `app/mcp_server/` module mounts 5 read/manage tools at `/mcp`.

**Tech Stack:** FastAPI (sync handlers), DuckDB, PyYAML, `anthropic==0.40.0`, `mcp[server]==1.0.0`, vanilla HTML/JS/CSS (no build step)

## Global Constraints

- Python 3.12; all route handlers are sync (FastAPI thread pool) — no async/await
- Model for all Claude API calls: `claude-sonnet-4-6` (non-negotiable)
- YAML parsed only with `yaml.safe_load()` — never `yaml.load()`, `eval()`, or `exec()`
- Parser/rule name path params must be validated: only `[a-z0-9][a-z0-9\-_]{0,63}` — prevents path traversal
- Built-in parsers and rules (not in `custom/`) are read-only — PUT/DELETE returns 403
- Role gate: GET endpoints require analyst+; POST/PUT/DELETE/generate require admin+
- All existing 34 tests must still pass after each task
- Version string: `"0.6.0"` (update in `config.py`)
- No new HTML framework — vanilla JS + CSS, IBM Plex Sans/Mono fonts, same CSS variables as existing pages
- File paths: rule YAMLs live in `app/rules/rules/` (double-nesting: `app/rules/` is the Python module, `rules/` subdir holds YAML). Decoder YAMLs live in `app/decoder/decoders/`.

---

## File Map

**Rename:**
- `app/decoder/decoders/nginx_access.yaml` → `nginx-access.yaml` (also update `name:` field inside)
- `app/rules/rules/http_404_spike.yaml` → `nginx-http-404-spike.yaml` (update `name:` field)
- `app/rules/rules/http_500_error.yaml` → `nginx-http-500-error.yaml` (update `name:` field)

**New directories:**
- `app/decoder/decoders/custom/` (with `.gitkeep`)
- `app/rules/rules/custom/` (with `.gitkeep`)

**New modules:**
- `app/parsers/__init__.py`, `app/parsers/router.py`
- `app/rules/router.py` (rules CRUD, alongside existing `engine.py`)
- `app/ai/__init__.py`, `app/ai/claude.py`
- `app/mcp_server/__init__.py`, `app/mcp_server/server.py`

**New UI:**
- `ui/parsers.html`, `ui/rules.html`

**New tests:**
- `app/tests/test_parsers.py`, `app/tests/test_rules_crud.py`

**Modified:**
- `app/decoder/engine.py` — add `decode_with()`, update glob to include `custom/`
- `app/rules/engine.py` — update glob to include `custom/`
- `app/config.py` — add `tinysiem_claude_api_key`, `tinysiem_mcp_enabled`, bump version
- `app/main.py` — add parsers/rules CRUD routers, conditional MCP mount
- `app/Dockerfile` — mkdir + chown custom dirs
- `app/requirements.txt` — add `anthropic==0.40.0`, `mcp[server]==1.0.0`
- `app/tests/conftest.py` — add `admin_headers` fixture, set `TINYSIEM_CLAUDE_API_KEY=""`
- `docker-compose.yml` — document new env vars
- `ui/dashboard.html`, `ui/events.html`, `ui/alerts.html`, `ui/users.html`, `ui/configuration.html` — wire nav links

---

## Task 1: File rename + custom dirs + engine updates

**Files:**
- Rename: `app/decoder/decoders/nginx_access.yaml`
- Rename: `app/rules/rules/http_404_spike.yaml`, `app/rules/rules/http_500_error.yaml`
- Modify: `app/decoder/engine.py`
- Modify: `app/rules/engine.py`
- Modify: `app/config.py`
- Modify: `app/Dockerfile`

**Interfaces:**
- Produces: `decoder_engine.decode_with(decoder: dict, raw: str) -> Optional[dict]` — used by Task 2's test endpoint

- [ ] **Step 1: Rename decoder YAML and update its internal name field**

Replace `app/decoder/decoders/nginx_access.yaml` with `app/decoder/decoders/nginx-access.yaml`. Change the `name:` field at the top:

```yaml
name: nginx-access
source: nginx
type: regex
pattern: '^(?P<remote_addr>\S+) \S+ \S+ \[(?P<time_local>[^\]]+)\] "(?P<request_method>\S+) (?P<request_uri>\S+) \S+" (?P<status>\d+) (?P<body_bytes_sent>\d+|-) "(?P<http_referer>[^"]*)" "(?P<http_user_agent>[^"]*)"'
fields:
  timestamp: time_local
  source_ip: remote_addr
  method: request_method
  uri: request_uri
  status_code: status
  response_size: body_bytes_sent
  user_agent: http_user_agent
  referer: http_referer
timestamp_field: timestamp
timestamp_format: '%d/%b/%Y:%H:%M:%S %z'
```

Delete the old `nginx_access.yaml`.

- [ ] **Step 2: Rename rule YAMLs**

Replace `app/rules/rules/http_404_spike.yaml` with `app/rules/rules/nginx-http-404-spike.yaml`:

```yaml
name: nginx-http-404-spike
description: "Multiple 404 responses from the same IP in a short window"
severity: medium
source: nginx
condition:
  type: threshold
  field: status_code
  value: 404
  operator: eq
  threshold_count: 10
  window_seconds: 60
mitre_tactic: "Discovery"
mitre_technique: "T1595"
```

Replace `app/rules/rules/http_500_error.yaml` with `app/rules/rules/nginx-http-500-error.yaml`:

```yaml
name: nginx-http-500-error
description: "Server-side error detected"
severity: high
source: nginx
condition:
  type: field_match
  field: status_code
  value: 500
  operator: eq
mitre_tactic: "Impact"
mitre_technique: "T1499"
```

Delete the old files.

- [ ] **Step 3: Create custom subdirectories**

```bash
mkdir -p app/decoder/decoders/custom app/rules/rules/custom
touch app/decoder/decoders/custom/.gitkeep app/rules/rules/custom/.gitkeep
```

- [ ] **Step 4: Update decoder engine — add `decode_with` and custom glob**

Edit `app/decoder/engine.py`. After the `_decode_kv` function, add:

```python
def decode_with(decoder: dict, raw: str) -> Optional[dict]:
    dtype = decoder.get("type", "regex")
    source = decoder.get("source", "_test")
    try:
        if dtype == "regex":
            return _decode_regex(decoder, source, raw)
        if dtype == "json":
            return _decode_json(decoder, source, raw)
        if dtype == "kv":
            return _decode_kv(decoder, source, raw)
    except Exception as exc:
        logger.warning(f"decode_with error: {exc}")
    return None
```

Update `load_decoders` to also load from `custom/`:

```python
def load_decoders(decoders_dir: Optional[Path] = None) -> None:
    global _decoders
    if decoders_dir is None:
        decoders_dir = Path(__file__).parent / "decoders"

    _decoders = {}
    for yaml_file in sorted(decoders_dir.glob("*.yaml")):
        try:
            with open(yaml_file) as f:
                decoder = yaml.safe_load(f)
            source = decoder.get("source")
            if source:
                _decoders[source] = decoder
                logger.info(f"Loaded decoder '{decoder.get('name')}' for source '{source}'")
        except Exception as exc:
            logger.warning(f"Failed to load decoder {yaml_file}: {exc}")

    custom_dir = decoders_dir / "custom"
    if custom_dir.exists():
        for yaml_file in sorted(custom_dir.glob("*.yaml")):
            try:
                with open(yaml_file) as f:
                    decoder = yaml.safe_load(f)
                source = decoder.get("source")
                if source:
                    _decoders[source] = decoder
                    logger.info(f"Loaded custom decoder '{decoder.get('name')}' for source '{source}'")
            except Exception as exc:
                logger.warning(f"Failed to load custom decoder {yaml_file}: {exc}")
```

- [ ] **Step 5: Update rules engine — add custom glob**

Edit `app/rules/engine.py`. Update `load_rules`:

```python
def load_rules(rules_dir: Optional[Path] = None) -> None:
    global _rules
    if rules_dir is None:
        rules_dir = Path(__file__).parent / "rules"

    _rules = []
    for yaml_file in sorted(rules_dir.glob("*.yaml")):
        try:
            with open(yaml_file) as f:
                rule = yaml.safe_load(f)
            _rules.append(rule)
            logger.info(f"Loaded rule '{rule.get('name')}'")
        except Exception as exc:
            logger.warning(f"Failed to load rule {yaml_file}: {exc}")

    custom_dir = rules_dir / "custom"
    if custom_dir.exists():
        for yaml_file in sorted(custom_dir.glob("*.yaml")):
            try:
                with open(yaml_file) as f:
                    rule = yaml.safe_load(f)
                _rules.append(rule)
                logger.info(f"Loaded custom rule '{rule.get('name')}'")
            except Exception as exc:
                logger.warning(f"Failed to load custom rule {yaml_file}: {exc}")
```

- [ ] **Step 6: Bump version and add new config fields**

Edit `app/config.py`:

```python
from pydantic_settings import BaseSettings


class Settings(BaseSettings):
    tinysiem_api_key: str
    tinysiem_debug: bool = False
    tinysiem_version: str = "0.6.0"
    tinysiem_duckdb_path: str = "/app/data/tinysiem.duckdb"
    tinysiem_chroma_path: str = "/app/data/chroma_store"
    tinysiem_alerts_path: str = "/app/data/alerts/alerts.log"
    tinysiem_alert_max_mb: int = 50
    tinysiem_jwt_secret: str
    tinysiem_jwt_expiry_hours: int = 24
    tinysiem_superadmin_password: str = "admin"
    tinysiem_claude_api_key: str = ""
    tinysiem_mcp_enabled: bool = False

    model_config = {"env_file": ".env", "env_file_encoding": "utf-8"}


settings = Settings()
```

- [ ] **Step 7: Update Dockerfile to create/chown custom dirs**

Replace the `RUN mkdir -p` line:

```dockerfile
RUN mkdir -p /app/data /app/logs /app/decoder/decoders/custom /app/rules/rules/custom \
    && chown -R appuser:appuser /app
```

- [ ] **Step 8: Add new env vars to docker-compose.yml**

```yaml
  tinysiem:
    build: ./app
    env_file: .env
    environment:
      TINYSIEM_CLAUDE_API_KEY: ""
      TINYSIEM_MCP_ENABLED: "false"
    volumes:
      - ./logs:/app/logs:ro
      - ./ui:/app/ui:ro
      - tinysiem_data:/app/data
    ports:
      - "8000:8000"
    depends_on:
      - nginx
```

- [ ] **Step 9: Verify all existing tests pass**

```bash
cd /Users/cahlasem/Documents/tinysiem
pytest app/tests/ -v
```

Expected: 34 passed. The decoder test still works because it calls `decode("nginx", ...)` which uses the `source: nginx` field — unaffected by the filename rename.

- [ ] **Step 10: Commit**

```bash
git add app/decoder/decoders/ app/rules/rules/ app/decoder/engine.py app/rules/engine.py app/config.py app/Dockerfile docker-compose.yml
git commit -m "feat: kebab-case YAML rename, custom dirs, decode_with, v0.6.0 config"
```

---

## Task 2: Parsers CRUD API

**Files:**
- Create: `app/parsers/__init__.py`
- Create: `app/parsers/router.py`
- Modify: `app/main.py`
- Modify: `app/tests/conftest.py`
- Create: `app/tests/test_parsers.py`

**Interfaces:**
- Consumes: `decoder_engine.load_decoders()`, `decoder_engine.decode_with(decoder, raw)` from Task 1
- Produces: `GET/POST /parsers`, `GET/PUT/DELETE /parsers/{name}`, `POST /parsers/{name}/test`

- [ ] **Step 1: Add `admin_headers` to conftest and set CLAUDE_API_KEY**

Edit `app/tests/conftest.py`. In the env-var block at the top, add:

```python
os.environ["TINYSIEM_CLAUDE_API_KEY"] = ""
os.environ["TINYSIEM_MCP_ENABLED"] = "false"
```

Add fixture after `analyst_headers`:

```python
@pytest.fixture
def admin_headers():
    from app.auth import create_token
    token = create_token("test-admin", "admin", "admin")
    return {"Authorization": f"Bearer {token}"}
```

- [ ] **Step 2: Write failing tests**

Create `app/tests/test_parsers.py`:

```python
from pathlib import Path
import pytest

DECODERS_DIR = Path(__file__).parent.parent / "decoder" / "decoders"
CUSTOM_DIR = DECODERS_DIR / "custom"

SAMPLE_LOG = (
    '203.0.113.42 - frank [10/Oct/2023:13:55:36 -0700] '
    '"GET /api/v1/users HTTP/1.1" 200 1234 '
    '"http://example.com/" "Mozilla/5.0"'
)

VALID_PARSER_YAML = """\
name: test-custom-parser
source: test-source
type: regex
pattern: '^(?P<msg>.+)$'
fields:
  raw_msg: msg
"""


@pytest.fixture(autouse=True)
def clean_custom_parsers():
    if CUSTOM_DIR.exists():
        for f in CUSTOM_DIR.glob("*.yaml"):
            f.unlink()
    yield
    if CUSTOM_DIR.exists():
        for f in CUSTOM_DIR.glob("*.yaml"):
            f.unlink()


async def test_list_parsers_returns_builtin(client, analyst_headers):
    r = await client.get("/parsers", headers=analyst_headers)
    assert r.status_code == 200
    data = r.json()
    names = [p["name"] for p in data["parsers"]]
    assert "nginx-access" in names


async def test_list_parsers_requires_auth(client):
    r = await client.get("/parsers")
    assert r.status_code == 401


async def test_get_parser_returns_yaml(client, analyst_headers):
    r = await client.get("/parsers/nginx-access", headers=analyst_headers)
    assert r.status_code == 200
    assert "source: nginx" in r.json()["yaml_text"]


async def test_get_parser_not_found(client, analyst_headers):
    r = await client.get("/parsers/nonexistent", headers=analyst_headers)
    assert r.status_code == 404


async def test_create_parser_admin(client, admin_headers):
    r = await client.post(
        "/parsers",
        json={"name": "test-custom-parser", "yaml_text": VALID_PARSER_YAML},
        headers=admin_headers,
    )
    assert r.status_code == 201
    assert (CUSTOM_DIR / "test-custom-parser.yaml").exists()


async def test_create_parser_analyst_forbidden(client, analyst_headers):
    r = await client.post(
        "/parsers",
        json={"name": "test-custom-parser", "yaml_text": VALID_PARSER_YAML},
        headers=analyst_headers,
    )
    assert r.status_code == 403


async def test_create_parser_invalid_yaml(client, admin_headers):
    r = await client.post(
        "/parsers",
        json={"name": "bad", "yaml_text": "not: valid: yaml: [[["},
        headers=admin_headers,
    )
    assert r.status_code == 422


async def test_create_parser_missing_keys(client, admin_headers):
    r = await client.post(
        "/parsers",
        json={"name": "bad", "yaml_text": "name: ok\nsource: nginx\n"},
        headers=admin_headers,
    )
    assert r.status_code == 422


async def test_create_parser_conflict(client, admin_headers):
    await client.post(
        "/parsers",
        json={"name": "test-custom-parser", "yaml_text": VALID_PARSER_YAML},
        headers=admin_headers,
    )
    r = await client.post(
        "/parsers",
        json={"name": "test-custom-parser", "yaml_text": VALID_PARSER_YAML},
        headers=admin_headers,
    )
    assert r.status_code == 409


async def test_update_custom_parser(client, admin_headers):
    await client.post(
        "/parsers",
        json={"name": "test-custom-parser", "yaml_text": VALID_PARSER_YAML},
        headers=admin_headers,
    )
    updated = VALID_PARSER_YAML.replace("raw_msg: msg", "message: msg")
    r = await client.put(
        "/parsers/test-custom-parser",
        json={"name": "test-custom-parser", "yaml_text": updated},
        headers=admin_headers,
    )
    assert r.status_code == 200


async def test_update_builtin_parser_forbidden(client, admin_headers):
    r = await client.put(
        "/parsers/nginx-access",
        json={"name": "nginx-access", "yaml_text": VALID_PARSER_YAML},
        headers=admin_headers,
    )
    assert r.status_code == 403


async def test_delete_custom_parser(client, admin_headers):
    await client.post(
        "/parsers",
        json={"name": "test-custom-parser", "yaml_text": VALID_PARSER_YAML},
        headers=admin_headers,
    )
    r = await client.delete("/parsers/test-custom-parser", headers=admin_headers)
    assert r.status_code == 204
    assert not (CUSTOM_DIR / "test-custom-parser.yaml").exists()


async def test_delete_builtin_parser_forbidden(client, admin_headers):
    r = await client.delete("/parsers/nginx-access", headers=admin_headers)
    assert r.status_code == 403


async def test_parser_test_endpoint_matches(client, analyst_headers):
    r = await client.post(
        "/parsers/nginx-access/test",
        json={"log_line": SAMPLE_LOG},
        headers=analyst_headers,
    )
    assert r.status_code == 200
    data = r.json()
    assert data["matched"] is True
    assert data["fields"]["source_ip"] == "203.0.113.42"


async def test_parser_test_endpoint_no_match(client, analyst_headers):
    r = await client.post(
        "/parsers/nginx-access/test",
        json={"log_line": "this is not nginx"},
        headers=analyst_headers,
    )
    assert r.status_code == 200
    assert r.json()["matched"] is False


async def test_invalid_parser_name_rejected(client, admin_headers):
    r = await client.post(
        "/parsers",
        json={"name": "../etc/passwd", "yaml_text": VALID_PARSER_YAML},
        headers=admin_headers,
    )
    assert r.status_code == 422
```

- [ ] **Step 3: Run tests — expect failures**

```bash
pytest app/tests/test_parsers.py -v 2>&1 | head -20
```

Expected: ImportError or 404 errors (router not mounted yet).

- [ ] **Step 4: Create `app/parsers/__init__.py`**

Empty file.

- [ ] **Step 5: Create `app/parsers/router.py`**

```python
import re
from pathlib import Path
from typing import Optional

import yaml
from fastapi import APIRouter, Depends, HTTPException, Response
from pydantic import BaseModel

from app.auth import AuthUser, require_admin, require_analyst
from app.decoder import engine as decoder_engine

router = APIRouter(prefix="/parsers", tags=["parsers"])

_DECODERS_DIR = Path(__file__).parent.parent / "decoder" / "decoders"
_CUSTOM_DIR = _DECODERS_DIR / "custom"
_REQUIRED_KEYS = {"name", "source", "type", "pattern", "fields"}
_VALID_NAME = re.compile(r'^[a-z0-9][a-z0-9\-_]{0,63}$')


def _check_name(name: str) -> None:
    if not _VALID_NAME.match(name):
        raise HTTPException(
            status_code=422,
            detail="Name must be lowercase alphanumeric, hyphens or underscores only",
        )


def _list_parser_files() -> list[tuple[Path, bool]]:
    files = []
    for p in sorted(_DECODERS_DIR.glob("*.yaml")):
        files.append((p, False))
    if _CUSTOM_DIR.exists():
        for p in sorted(_CUSTOM_DIR.glob("*.yaml")):
            files.append((p, True))
    return files


def _get_parser_file(name: str) -> tuple[Optional[Path], bool]:
    custom = _CUSTOM_DIR / f"{name}.yaml"
    if custom.exists():
        return custom, True
    builtin = _DECODERS_DIR / f"{name}.yaml"
    if builtin.exists():
        return builtin, False
    return None, False


def _validate_parser_yaml(yaml_text: str) -> dict:
    try:
        data = yaml.safe_load(yaml_text)
    except yaml.YAMLError as exc:
        raise HTTPException(status_code=422, detail=f"Invalid YAML: {exc}")
    if not isinstance(data, dict):
        raise HTTPException(status_code=422, detail="YAML must be a mapping")
    missing = _REQUIRED_KEYS - data.keys()
    if missing:
        raise HTTPException(
            status_code=422,
            detail=f"Missing required keys: {', '.join(sorted(missing))}",
        )
    return data


class ParserRequest(BaseModel):
    name: str
    yaml_text: str


class TestRequest(BaseModel):
    log_line: str


@router.get("")
def list_parsers(_: AuthUser = Depends(require_analyst)):
    result = []
    for path, is_custom in _list_parser_files():
        try:
            data = yaml.safe_load(path.read_text())
            result.append({
                "name": path.stem,
                "source": data.get("source", ""),
                "type": data.get("type", ""),
                "is_custom": is_custom,
            })
        except Exception:
            pass
    return {"parsers": result}


@router.get("/{name}")
def get_parser(name: str, _: AuthUser = Depends(require_analyst)):
    _check_name(name)
    path, _ = _get_parser_file(name)
    if not path:
        raise HTTPException(status_code=404, detail="Parser not found")
    return {"name": name, "yaml_text": path.read_text()}


@router.post("", status_code=201)
def create_parser(req: ParserRequest, _: AuthUser = Depends(require_admin)):
    _check_name(req.name)
    _validate_parser_yaml(req.yaml_text)
    dest = _CUSTOM_DIR / f"{req.name}.yaml"
    if dest.exists():
        raise HTTPException(status_code=409, detail="Parser already exists")
    _CUSTOM_DIR.mkdir(exist_ok=True)
    dest.write_text(req.yaml_text)
    decoder_engine.load_decoders()
    return {"name": req.name, "status": "created"}


@router.put("/{name}")
def update_parser(name: str, req: ParserRequest, _: AuthUser = Depends(require_admin)):
    _check_name(name)
    path, is_custom = _get_parser_file(name)
    if not path:
        raise HTTPException(status_code=404, detail="Parser not found")
    if not is_custom:
        raise HTTPException(status_code=403, detail="Cannot modify built-in parsers")
    _validate_parser_yaml(req.yaml_text)
    path.write_text(req.yaml_text)
    decoder_engine.load_decoders()
    return {"name": name, "status": "updated"}


@router.delete("/{name}", status_code=204)
def delete_parser(name: str, _: AuthUser = Depends(require_admin)):
    _check_name(name)
    path, is_custom = _get_parser_file(name)
    if not path:
        raise HTTPException(status_code=404, detail="Parser not found")
    if not is_custom:
        raise HTTPException(status_code=403, detail="Cannot delete built-in parsers")
    path.unlink()
    decoder_engine.load_decoders()
    return Response(status_code=204)


@router.post("/{name}/test")
def test_parser(name: str, req: TestRequest, _: AuthUser = Depends(require_analyst)):
    _check_name(name)
    path, _ = _get_parser_file(name)
    if not path:
        raise HTTPException(status_code=404, detail="Parser not found")
    try:
        decoder = yaml.safe_load(path.read_text())
    except yaml.YAMLError as exc:
        raise HTTPException(status_code=422, detail=f"Parser YAML invalid: {exc}")
    event = decoder_engine.decode_with(decoder, req.log_line)
    if event is None:
        return {"matched": False, "fields": {}}
    fields = {k: v for k, v in event.items() if k not in ("id", "ingested_at", "raw", "source")}
    return {"matched": True, "fields": fields}
```

- [ ] **Step 6: Wire router into `main.py`**

Add import after existing router imports:

```python
from app.parsers.router import router as parsers_router
```

Add `app.include_router(parsers_router)` after `app.include_router(users_router)`.

- [ ] **Step 7: Run tests**

```bash
pytest app/tests/test_parsers.py app/tests/ -v
```

Expected: all parser tests pass; all 34 existing tests still pass.

- [ ] **Step 8: Commit**

```bash
git add app/parsers/ app/main.py app/tests/conftest.py app/tests/test_parsers.py
git commit -m "feat: parsers CRUD API with hot reload and test endpoint"
```

---

## Task 3: Rules CRUD API

**Files:**
- Create: `app/rules/router.py`
- Modify: `app/main.py`
- Create: `app/tests/test_rules_crud.py`

**Interfaces:**
- Consumes: `rule_engine.load_rules()` from Task 1
- Produces: `GET/POST /rules`, `GET/PUT/DELETE /rules/{name}`, `POST /rules/generate` (generate added in Task 4)

- [ ] **Step 1: Write failing tests**

Create `app/tests/test_rules_crud.py`:

```python
from pathlib import Path
import pytest

RULES_DIR = Path(__file__).parent.parent / "rules" / "rules"
CUSTOM_DIR = RULES_DIR / "custom"

VALID_RULE_YAML = """\
name: test-custom-rule
severity: low
source: nginx
condition:
  type: field_match
  field: status_code
  value: 999
  operator: eq
"""


@pytest.fixture(autouse=True)
def clean_custom_rules():
    if CUSTOM_DIR.exists():
        for f in CUSTOM_DIR.glob("*.yaml"):
            f.unlink()
    yield
    if CUSTOM_DIR.exists():
        for f in CUSTOM_DIR.glob("*.yaml"):
            f.unlink()


async def test_list_rules_returns_builtins(client, analyst_headers):
    r = await client.get("/rules", headers=analyst_headers)
    assert r.status_code == 200
    names = [rule["name"] for rule in r.json()["rules"]]
    assert "nginx-http-404-spike" in names
    assert "nginx-http-500-error" in names


async def test_list_rules_requires_auth(client):
    r = await client.get("/rules")
    assert r.status_code == 401


async def test_get_rule_returns_yaml(client, analyst_headers):
    r = await client.get("/rules/nginx-http-404-spike", headers=analyst_headers)
    assert r.status_code == 200
    assert "threshold" in r.json()["yaml_text"]


async def test_get_rule_not_found(client, analyst_headers):
    r = await client.get("/rules/nonexistent", headers=analyst_headers)
    assert r.status_code == 404


async def test_create_rule_admin(client, admin_headers):
    r = await client.post(
        "/rules",
        json={"name": "test-custom-rule", "yaml_text": VALID_RULE_YAML},
        headers=admin_headers,
    )
    assert r.status_code == 201
    assert (CUSTOM_DIR / "test-custom-rule.yaml").exists()


async def test_create_rule_analyst_forbidden(client, analyst_headers):
    r = await client.post(
        "/rules",
        json={"name": "test-custom-rule", "yaml_text": VALID_RULE_YAML},
        headers=analyst_headers,
    )
    assert r.status_code == 403


async def test_create_rule_missing_keys(client, admin_headers):
    r = await client.post(
        "/rules",
        json={"name": "bad", "yaml_text": "name: ok\n"},
        headers=admin_headers,
    )
    assert r.status_code == 422


async def test_update_custom_rule(client, admin_headers):
    await client.post(
        "/rules",
        json={"name": "test-custom-rule", "yaml_text": VALID_RULE_YAML},
        headers=admin_headers,
    )
    updated = VALID_RULE_YAML.replace("value: 999", "value: 888")
    r = await client.put(
        "/rules/test-custom-rule",
        json={"name": "test-custom-rule", "yaml_text": updated},
        headers=admin_headers,
    )
    assert r.status_code == 200


async def test_update_builtin_rule_forbidden(client, admin_headers):
    r = await client.put(
        "/rules/nginx-http-404-spike",
        json={"name": "nginx-http-404-spike", "yaml_text": VALID_RULE_YAML},
        headers=admin_headers,
    )
    assert r.status_code == 403


async def test_delete_custom_rule(client, admin_headers):
    await client.post(
        "/rules",
        json={"name": "test-custom-rule", "yaml_text": VALID_RULE_YAML},
        headers=admin_headers,
    )
    r = await client.delete("/rules/test-custom-rule", headers=admin_headers)
    assert r.status_code == 204
    assert not (CUSTOM_DIR / "test-custom-rule.yaml").exists()


async def test_delete_builtin_rule_forbidden(client, admin_headers):
    r = await client.delete("/rules/nginx-http-404-spike", headers=admin_headers)
    assert r.status_code == 403


async def test_invalid_rule_name_rejected(client, admin_headers):
    r = await client.post(
        "/rules",
        json={"name": "../../etc/passwd", "yaml_text": VALID_RULE_YAML},
        headers=admin_headers,
    )
    assert r.status_code == 422
```

- [ ] **Step 2: Run tests — expect failures**

```bash
pytest app/tests/test_rules_crud.py -v 2>&1 | head -10
```

Expected: 404 or ImportError (router not mounted).

- [ ] **Step 3: Create `app/rules/router.py`**

```python
import re
from pathlib import Path
from typing import Optional

import yaml
from fastapi import APIRouter, Depends, HTTPException, Response
from pydantic import BaseModel

from app.auth import AuthUser, require_admin, require_analyst
from app.rules import engine as rule_engine

router = APIRouter(prefix="/rules", tags=["rules"])

_RULES_DIR = Path(__file__).parent / "rules"
_CUSTOM_DIR = _RULES_DIR / "custom"
_REQUIRED_KEYS = {"name", "severity", "source", "condition"}
_VALID_SEVERITIES = {"low", "medium", "high", "critical"}
_VALID_NAME = re.compile(r'^[a-z0-9][a-z0-9\-_]{0,63}$')


def _check_name(name: str) -> None:
    if not _VALID_NAME.match(name):
        raise HTTPException(
            status_code=422,
            detail="Name must be lowercase alphanumeric, hyphens or underscores only",
        )


def _list_rule_files() -> list[tuple[Path, bool]]:
    files = []
    for p in sorted(_RULES_DIR.glob("*.yaml")):
        files.append((p, False))
    if _CUSTOM_DIR.exists():
        for p in sorted(_CUSTOM_DIR.glob("*.yaml")):
            files.append((p, True))
    return files


def _get_rule_file(name: str) -> tuple[Optional[Path], bool]:
    custom = _CUSTOM_DIR / f"{name}.yaml"
    if custom.exists():
        return custom, True
    builtin = _RULES_DIR / f"{name}.yaml"
    if builtin.exists():
        return builtin, False
    return None, False


def _validate_rule_yaml(yaml_text: str) -> dict:
    try:
        data = yaml.safe_load(yaml_text)
    except yaml.YAMLError as exc:
        raise HTTPException(status_code=422, detail=f"Invalid YAML: {exc}")
    if not isinstance(data, dict):
        raise HTTPException(status_code=422, detail="YAML must be a mapping")
    missing = _REQUIRED_KEYS - data.keys()
    if missing:
        raise HTTPException(
            status_code=422,
            detail=f"Missing required keys: {', '.join(sorted(missing))}",
        )
    sev = data.get("severity", "")
    if sev not in _VALID_SEVERITIES:
        raise HTTPException(
            status_code=422,
            detail=f"severity must be one of: {', '.join(sorted(_VALID_SEVERITIES))}",
        )
    return data


class RuleRequest(BaseModel):
    name: str
    yaml_text: str


@router.get("")
def list_rules(_: AuthUser = Depends(require_analyst)):
    result = []
    for path, is_custom in _list_rule_files():
        try:
            data = yaml.safe_load(path.read_text())
            result.append({
                "name": path.stem,
                "severity": data.get("severity", ""),
                "source": data.get("source", ""),
                "is_custom": is_custom,
            })
        except Exception:
            pass
    return {"rules": result}


@router.get("/{name}")
def get_rule(name: str, _: AuthUser = Depends(require_analyst)):
    _check_name(name)
    path, _ = _get_rule_file(name)
    if not path:
        raise HTTPException(status_code=404, detail="Rule not found")
    return {"name": name, "yaml_text": path.read_text()}


@router.post("", status_code=201)
def create_rule(req: RuleRequest, _: AuthUser = Depends(require_admin)):
    _check_name(req.name)
    _validate_rule_yaml(req.yaml_text)
    dest = _CUSTOM_DIR / f"{req.name}.yaml"
    if dest.exists():
        raise HTTPException(status_code=409, detail="Rule already exists")
    _CUSTOM_DIR.mkdir(exist_ok=True)
    dest.write_text(req.yaml_text)
    rule_engine.load_rules()
    return {"name": req.name, "status": "created"}


@router.put("/{name}")
def update_rule(name: str, req: RuleRequest, _: AuthUser = Depends(require_admin)):
    _check_name(name)
    path, is_custom = _get_rule_file(name)
    if not path:
        raise HTTPException(status_code=404, detail="Rule not found")
    if not is_custom:
        raise HTTPException(status_code=403, detail="Cannot modify built-in rules")
    _validate_rule_yaml(req.yaml_text)
    path.write_text(req.yaml_text)
    rule_engine.load_rules()
    return {"name": name, "status": "updated"}


@router.delete("/{name}", status_code=204)
def delete_rule(name: str, _: AuthUser = Depends(require_admin)):
    _check_name(name)
    path, is_custom = _get_rule_file(name)
    if not path:
        raise HTTPException(status_code=404, detail="Rule not found")
    if not is_custom:
        raise HTTPException(status_code=403, detail="Cannot delete built-in rules")
    path.unlink()
    rule_engine.load_rules()
    return Response(status_code=204)
```

- [ ] **Step 4: Wire router into `main.py`**

Add import:
```python
from app.rules.router import router as rules_crud_router
```

Add `app.include_router(rules_crud_router)` after `app.include_router(parsers_router)`.

- [ ] **Step 5: Run all tests**

```bash
pytest app/tests/ -v
```

Expected: all rules CRUD tests pass; 34 existing tests still pass.

- [ ] **Step 6: Commit**

```bash
git add app/rules/router.py app/main.py app/tests/test_rules_crud.py
git commit -m "feat: rules CRUD API with hot reload"
```

---

## Task 4: AI Integration

**Files:**
- Create: `app/ai/__init__.py`
- Create: `app/ai/claude.py`
- Modify: `app/parsers/router.py`
- Modify: `app/rules/router.py`
- Modify: `app/requirements.txt`

**Interfaces:**
- Produces: `POST /parsers/generate` → `{"yaml_text": str, "preview": true}`
- Produces: `POST /rules/generate` → `{"yaml_text": str, "preview": true}`

- [ ] **Step 1: Add anthropic to requirements.txt**

Add this line to `app/requirements.txt`:

```
anthropic==0.40.0
```

- [ ] **Step 2: Write failing tests for generate endpoints**

Add to `app/tests/test_parsers.py`:

```python
async def test_generate_parser_no_api_key(client, admin_headers):
    r = await client.post(
        "/parsers/generate",
        json={"log_sample": "192.168.1.1 GET /test 200"},
        headers=admin_headers,
    )
    assert r.status_code == 503
    assert "TINYSIEM_CLAUDE_API_KEY" in r.json()["detail"]


async def test_generate_parser_analyst_forbidden(client, analyst_headers):
    r = await client.post(
        "/parsers/generate",
        json={"log_sample": "192.168.1.1 GET /test 200"},
        headers=analyst_headers,
    )
    assert r.status_code == 403
```

Add to `app/tests/test_rules_crud.py`:

```python
async def test_generate_rule_no_api_key(client, admin_headers):
    r = await client.post(
        "/rules/generate",
        json={"description": "alert when 500 errors spike", "source": "nginx"},
        headers=admin_headers,
    )
    assert r.status_code == 503
    assert "TINYSIEM_CLAUDE_API_KEY" in r.json()["detail"]
```

- [ ] **Step 3: Run tests — expect failures**

```bash
pytest app/tests/test_parsers.py::test_generate_parser_no_api_key app/tests/test_rules_crud.py::test_generate_rule_no_api_key -v
```

Expected: 404 (endpoints not defined yet).

- [ ] **Step 4: Create `app/ai/__init__.py`**

Empty file.

- [ ] **Step 5: Create `app/ai/claude.py`**

```python
import logging
from typing import Optional

from app.config import settings

logger = logging.getLogger(__name__)

_PARSER_SYSTEM_PROMPT = """\
You are a TinySIEM parser generator. Output ONLY valid YAML — no prose, no code fences, no explanation.

The decoder YAML format is:
  name: <kebab-case-name>      # e.g. nginx-access
  source: <source-name>        # matches the source field in ingest requests
  type: regex                  # regex | json | kv
  pattern: '<regex>'           # named capture groups used by fields mapping
  fields:                      # maps normalized field names to capture group names
    source_ip: <group_name>
    method: <group_name>
    uri: <group_name>
    status_code: <group_name>
    response_size: <group_name>
    user_agent: <group_name>
    referer: <group_name>
  timestamp_field: <field_key>   # key from fields: that holds the timestamp
  timestamp_format: '<strptime>' # e.g. '%d/%b/%Y:%H:%M:%S %z'

Rules:
- Use named capture groups (?P<name>...)
- Only include fields that are present in the sample
- The regex must match the full line from the start (use ^)
- Output the YAML and nothing else
"""

_RULE_SYSTEM_PROMPT = """\
You are a TinySIEM detection rule generator. Output ONLY valid YAML — no prose, no code fences.

The rule YAML format is:
  name: <kebab-case-name>       # e.g. nginx-http-404-spike
  severity: low|medium|high|critical
  source: <source-name>         # must match an existing parser's source field
  condition:
    type: threshold             # threshold: count of events in window
    field: <field_name>         # one of: source, source_ip, method, uri, status_code, response_size
    value: <match_value>
    operator: eq|neq|gt|gte|lt|lte|contains
    threshold_count: <int>      # fire when count >= this
    window_seconds: <int>
  # OR for single-event match:
  condition:
    type: field_match
    field: <field_name>
    value: <match_value>
    operator: eq|neq|gt|gte|lt|lte|contains
  mitre_tactic: "<tactic>"     # optional
  mitre_technique: "<T-code>"  # optional

Output the YAML and nothing else.
"""


def _get_client():
    if not settings.tinysiem_claude_api_key:
        raise RuntimeError("TINYSIEM_CLAUDE_API_KEY not set")
    import anthropic
    return anthropic.Anthropic(api_key=settings.tinysiem_claude_api_key)


def generate_parser(log_sample: str) -> str:
    client = _get_client()
    response = client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=2048,
        system=_PARSER_SYSTEM_PROMPT,
        messages=[{"role": "user", "content": f"Generate a decoder YAML for this log sample:\n\n{log_sample}"}],
    )
    return response.content[0].text.strip()


def generate_rule(description: str, source: str) -> str:
    client = _get_client()
    response = client.messages.create(
        model="claude-sonnet-4-6",
        max_tokens=1024,
        system=_RULE_SYSTEM_PROMPT,
        messages=[{
            "role": "user",
            "content": f"Source: {source}\n\nGenerate a detection rule for: {description}",
        }],
    )
    return response.content[0].text.strip()
```

- [ ] **Step 6: Add `POST /parsers/generate` to parsers router**

Add to `app/parsers/router.py`:

```python
class GenerateParserRequest(BaseModel):
    log_sample: str


@router.post("/generate")
def generate_parser_endpoint(req: GenerateParserRequest, _: AuthUser = Depends(require_admin)):
    from app.ai.claude import generate_parser
    try:
        yaml_text = generate_parser(req.log_sample)
    except RuntimeError as exc:
        raise HTTPException(status_code=503, detail=str(exc))
    except Exception as exc:
        raise HTTPException(status_code=502, detail=f"Claude API error: {exc}")
    try:
        _validate_parser_yaml(yaml_text)
    except HTTPException:
        raise HTTPException(
            status_code=422,
            detail=f"Generated YAML failed validation. Raw output:\n{yaml_text}",
        )
    return {"yaml_text": yaml_text, "preview": True}
```

**Important:** add this route BEFORE the `/{name}` route in the file, otherwise FastAPI will try to match `generate` as a `{name}` parameter. Place it immediately after the `POST ""` route handler.

- [ ] **Step 7: Add `POST /rules/generate` to rules router**

Add to `app/rules/router.py`:

```python
class GenerateRuleRequest(BaseModel):
    description: str
    source: str


@router.post("/generate")
def generate_rule_endpoint(req: GenerateRuleRequest, _: AuthUser = Depends(require_admin)):
    from app.ai.claude import generate_rule
    try:
        yaml_text = generate_rule(req.description, req.source)
    except RuntimeError as exc:
        raise HTTPException(status_code=503, detail=str(exc))
    except Exception as exc:
        raise HTTPException(status_code=502, detail=f"Claude API error: {exc}")
    try:
        _validate_rule_yaml(yaml_text)
    except HTTPException:
        raise HTTPException(
            status_code=422,
            detail=f"Generated YAML failed validation. Raw output:\n{yaml_text}",
        )
    return {"yaml_text": yaml_text, "preview": True}
```

Place this BEFORE the `/{name}` GET route.

- [ ] **Step 8: Run all tests**

```bash
pytest app/tests/ -v
```

Expected: generate 503 tests pass; all prior tests still pass.

- [ ] **Step 9: Commit**

```bash
git add app/ai/ app/parsers/router.py app/rules/router.py app/requirements.txt
git commit -m "feat: AI parser/rule generation via Claude API (503 when no key)"
```

---

## Task 5: MCP Server

**Files:**
- Create: `app/mcp_server/__init__.py`
- Create: `app/mcp_server/server.py`
- Modify: `app/main.py`
- Modify: `app/requirements.txt`

**Interfaces:**
- Produces: `GET /mcp` — SSE endpoint (5 tools: `list_events`, `get_alerts`, `list_parsers`, `list_rules`, `get_health`)
- Auth: JWT Bearer required; returns 401 if missing/invalid

- [ ] **Step 1: Add mcp to requirements.txt**

Add to `app/requirements.txt`:

```
mcp[server]==1.0.0
```

- [ ] **Step 2: Create `app/mcp_server/__init__.py`**

Empty file.

- [ ] **Step 3: Create `app/mcp_server/server.py`**

```python
import logging
from pathlib import Path
from typing import Optional

import yaml
from starlette.middleware.base import BaseHTTPMiddleware
from starlette.responses import JSONResponse

from app.auth import decode_token

logger = logging.getLogger(__name__)


class _JWTMiddleware(BaseHTTPMiddleware):
    async def dispatch(self, request, call_next):
        auth = request.headers.get("Authorization", "")
        if auth.startswith("Bearer "):
            payload = decode_token(auth[7:])
            if payload:
                return await call_next(request)
        return JSONResponse({"error": "Unauthorized"}, status_code=401)


def build_mcp_app():
    from mcp.server.fastmcp import FastMCP
    from app.storage import duckdb_store
    from app.alerts.router import _read_all_alerts
    from app.decoder import engine as decoder_engine
    from app.rules import engine as rule_engine
    from app.config import settings

    mcp = FastMCP("TinySIEM")

    @mcp.tool()
    def list_events(
        source: str = "",
        source_ip: str = "",
        q: str = "",
        limit: int = 50,
    ) -> dict:
        """Search events. Returns list of matching events."""
        result = duckdb_store.query_events(
            source=source or None,
            source_ip=source_ip or None,
            q=q or None,
            limit=min(limit, 200),
        )
        return result

    @mcp.tool()
    def get_alerts(
        severity: str = "",
        rule_name: str = "",
        limit: int = 50,
    ) -> dict:
        """Search alerts. Returns list of matching alerts."""
        alerts = _read_all_alerts()
        if severity:
            alerts = [a for a in alerts if (a.get("severity") or "").lower() == severity.lower()]
        if rule_name:
            alerts = [a for a in alerts if rule_name.lower() in (a.get("rule_name") or "").lower()]
        alerts.sort(key=lambda a: a.get("triggered_at", ""), reverse=True)
        return {"total": len(alerts), "alerts": alerts[:min(limit, 200)]}

    @mcp.tool()
    def list_parsers() -> dict:
        """List all loaded parsers (built-in and custom)."""
        result = []
        for source, decoder in decoder_engine._decoders.items():
            result.append({
                "name": decoder.get("name", source),
                "source": source,
                "type": decoder.get("type", "regex"),
            })
        return {"parsers": result}

    @mcp.tool()
    def list_rules() -> dict:
        """List all loaded detection rules."""
        result = [
            {
                "name": r.get("name", ""),
                "severity": r.get("severity", ""),
                "source": r.get("source", ""),
            }
            for r in rule_engine._rules
        ]
        return {"rules": result}

    @mcp.tool()
    def get_health() -> dict:
        """Return instance health and summary stats."""
        event_data = duckdb_store.query_events(limit=1)
        alert_count = len(_read_all_alerts())
        return {
            "status": "ok",
            "version": settings.tinysiem_version,
            "event_count": event_data.get("total", 0),
            "alert_count": alert_count,
        }

    raw_app = mcp.asgi_app()
    raw_app.add_middleware(_JWTMiddleware)
    return raw_app
```

- [ ] **Step 4: Wire MCP mount into `main.py`**

Add at the end of `main.py`, after the `/health` route and before the final return:

```python
if settings.tinysiem_mcp_enabled:
    from app.mcp_server.server import build_mcp_app
    app.mount("/mcp", build_mcp_app())
```

- [ ] **Step 5: Verify all tests still pass (MCP disabled in test env)**

```bash
pytest app/tests/ -v
```

Expected: all pass (`TINYSIEM_MCP_ENABLED=false` in conftest so `/mcp` is not mounted, no MCP imports execute).

- [ ] **Step 6: Commit**

```bash
git add app/mcp_server/ app/main.py app/requirements.txt
git commit -m "feat: MCP server with 5 tools, JWT auth, conditional mount"
```

---

## Task 6: UI — parsers.html + rules.html + nav wiring

**Files:**
- Create: `ui/parsers.html`
- Create: `ui/rules.html`
- Modify: `ui/dashboard.html`, `ui/events.html`, `ui/alerts.html`, `ui/users.html`, `ui/configuration.html`

**Interfaces:**
- Consumes: `GET/POST/PUT/DELETE /parsers`, `POST /parsers/generate`, `POST /parsers/{name}/test`
- Consumes: `GET/POST/PUT/DELETE /rules`, `POST /rules/generate`

- [ ] **Step 1: Wire nav links in existing pages**

In each of the 5 existing pages (`dashboard.html`, `events.html`, `alerts.html`, `users.html`, `configuration.html`), find the Parsers and Rules nav links and add `href` attributes. Look for `<a` elements or nav buttons with text "Parsers" and "Rules" and set:

```html
href="/ui/parsers.html"   <!-- for Parsers nav item -->
href="/ui/rules.html"     <!-- for Rules nav item -->
```

The exact element form depends on how v0.4 wired the nav. Search for "Parsers" in the HTML and add the href.

- [ ] **Step 2: Create `ui/parsers.html`**

Full file:

```html
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>TinySIEM — Parsers</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#0d0e17;--surface:#12131f;--surface2:#191a2d;--border:#252840;--border2:#323560;
  --text:#cdd0eb;--text-dim:#7b7fa8;--text-muted:#454870;
  --accent:#4d9fff;--accent-dim:#0d2a50;--err:#ef4444;--ok:#22c55e;
  --sans:'IBM Plex Sans',sans-serif;--mono:'IBM Plex Mono',monospace;
}
[data-theme="light"]{
  --bg:#f0f2f8;--surface:#ffffff;--surface2:#f5f7fc;--border:#dde0f0;--border2:#c0c6e0;
  --text:#1a1c2e;--text-dim:#4a4f6a;--text-muted:#9096b8;--accent:#2979ff;--accent-dim:#e3ecff;
}
html,body{height:100%;background:var(--bg);color:var(--text);font-family:var(--sans);font-size:13px}
.layout{display:flex;height:100vh}
/* Sidebar */
.sidebar{width:200px;flex-shrink:0;background:var(--surface);border-right:1px solid var(--border);display:flex;flex-direction:column;padding:16px 0}
.logo{display:flex;align-items:center;gap:8px;padding:0 16px 16px;font-size:15px;font-weight:600;border-bottom:1px solid var(--border);margin-bottom:8px}
.logo svg{color:var(--accent)}
.nav-item{display:block;padding:7px 16px;color:var(--text-dim);text-decoration:none;border-radius:0;transition:background .1s,color .1s;font-size:12px;font-weight:500}
.nav-item:hover,.nav-item.active{background:var(--accent-dim);color:var(--accent)}
.sidebar-footer{margin-top:auto;padding:12px 16px;border-top:1px solid var(--border);font-size:11px;color:var(--text-muted)}
/* Main */
.main{flex:1;display:flex;flex-direction:column;overflow:hidden}
.topbar{padding:12px 20px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;background:var(--surface)}
.topbar h1{font-size:14px;font-weight:600}
.topbar-actions{display:flex;gap:8px;align-items:center}
.btn{padding:6px 12px;border-radius:4px;border:none;font-size:12px;font-weight:600;cursor:pointer;transition:opacity .15s}
.btn-primary{background:var(--accent);color:#fff}
.btn-secondary{background:var(--surface2);color:var(--text);border:1px solid var(--border2)}
.btn:hover{opacity:.85}
.btn:disabled{opacity:.45;cursor:not-allowed}
.content{flex:1;display:flex;overflow:hidden}
/* Parser list */
.list-panel{width:280px;flex-shrink:0;border-right:1px solid var(--border);overflow-y:auto;display:flex;flex-direction:column}
.list-panel table{width:100%;border-collapse:collapse}
.list-panel th{padding:8px 12px;font-size:10px;font-weight:600;text-transform:uppercase;letter-spacing:.06em;color:var(--text-muted);border-bottom:1px solid var(--border);background:var(--surface);position:sticky;top:0}
.list-panel td{padding:8px 12px;border-bottom:1px solid var(--border);vertical-align:middle;cursor:pointer}
tr.selected td,.list-panel tr:hover td{background:var(--accent-dim)}
.badge{display:inline-block;padding:2px 6px;border-radius:3px;font-size:10px;font-weight:600;background:var(--surface2);color:var(--text-dim);font-family:var(--mono)}
.badge-custom{background:#1a2a1a;color:var(--ok)}
/* Detail panel */
.detail-panel{flex:1;overflow-y:auto;padding:20px;display:flex;flex-direction:column;gap:16px}
.detail-panel.empty{align-items:center;justify-content:center;color:var(--text-muted)}
.section-title{font-size:11px;font-weight:600;text-transform:uppercase;letter-spacing:.06em;color:var(--text-muted);margin-bottom:8px}
.yaml-area{width:100%;min-height:200px;background:var(--surface2);border:1px solid var(--border2);border-radius:4px;padding:10px;color:var(--text);font-family:var(--mono);font-size:12px;line-height:1.5;resize:vertical;outline:none}
.yaml-area:focus{border-color:var(--accent)}
.yaml-area[readonly]{opacity:.7;cursor:default}
.row-actions{display:flex;gap:8px}
.msg{font-size:12px;padding:6px 10px;border-radius:4px;display:none}
.msg-ok{background:#0d2a0d;color:var(--ok)}
.msg-err{background:#2a0d0d;color:var(--err)}
.divider{border:none;border-top:1px solid var(--border);margin:4px 0}
.log-line-input{width:100%;background:var(--surface2);border:1px solid var(--border2);border-radius:4px;padding:7px 10px;color:var(--text);font-family:var(--mono);font-size:12px;outline:none}
.log-line-input:focus{border-color:var(--accent)}
.test-result{background:var(--surface2);border:1px solid var(--border);border-radius:4px;padding:10px;font-family:var(--mono);font-size:12px;min-height:48px;color:var(--text-dim)}
.theme-btn{background:none;border:1px solid var(--border);border-radius:4px;padding:4px 8px;color:var(--text-dim);cursor:pointer;font-size:13px}
</style>
</head>
<body>
<div class="layout">
  <aside class="sidebar">
    <div class="logo">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
      TinySIEM
    </div>
    <a class="nav-item" href="/ui/dashboard.html">Dashboard</a>
    <a class="nav-item" href="/ui/events.html">Events</a>
    <a class="nav-item" href="/ui/alerts.html">Alerts</a>
    <a class="nav-item active" href="/ui/parsers.html">Parsers</a>
    <a class="nav-item" href="/ui/rules.html">Rules</a>
    <a class="nav-item" href="/ui/configuration.html">Configuration</a>
    <div class="sidebar-footer">
      <div id="navUser"></div>
      <a href="#" onclick="logout()" style="color:var(--text-muted);text-decoration:none;font-size:11px">Sign out</a>
    </div>
  </aside>
  <div class="main">
    <div class="topbar">
      <h1>Parsers</h1>
      <div class="topbar-actions">
        <button class="theme-btn" onclick="toggleTheme()">◑</button>
        <button class="btn btn-primary" id="newBtn" onclick="newParser()" style="display:none">+ New Parser</button>
      </div>
    </div>
    <div class="content">
      <div class="list-panel">
        <table>
          <thead><tr><th>Name</th><th>Source</th><th>Type</th></tr></thead>
          <tbody id="parserList"></tbody>
        </table>
      </div>
      <div class="detail-panel empty" id="detailPanel">
        <span>Select a parser to view or edit</span>
      </div>
    </div>
  </div>
</div>
<script>
const TH = document.documentElement;
const S = {ep:'', jwt:'', role:'', selected:null};

function toggleTheme(){const t=TH.getAttribute('data-theme')==='dark'?'light':'dark';TH.setAttribute('data-theme',t);localStorage.setItem('ts_theme',t)}
function parseJwt(t){try{const b=t.split('.')[1].replace(/-/g,'+').replace(/_/g,'/');return JSON.parse(atob(b))}catch{return null}}
function logout(){localStorage.removeItem('ts_jwt');localStorage.removeItem('ts_username');localStorage.removeItem('ts_role');window.location.href='/ui/login.html'}
function esc(s){return String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}

async function api(path, opts={}){
  const res = await fetch(S.ep+path, {headers:{'Authorization':'Bearer '+S.jwt,'Content-Type':'application/json'}, ...opts});
  if(res.status===401){logout();return null}
  return res;
}

async function loadParsers(){
  const r = await api('/parsers');
  if(!r)return;
  const data = await r.json();
  const tbody = document.getElementById('parserList');
  tbody.innerHTML = data.parsers.map(p=>`
    <tr onclick="selectParser('${esc(p.name)}',${p.is_custom})" ${S.selected===p.name?'class="selected"':''}>
      <td>${esc(p.name)}</td>
      <td>${esc(p.source)}</td>
      <td><span class="badge ${p.is_custom?'badge-custom':''}">${esc(p.type)}</span></td>
    </tr>`).join('');
}

async function selectParser(name, isCustom){
  S.selected = name;
  const r = await api('/parsers/'+encodeURIComponent(name));
  if(!r)return;
  const data = await r.json();
  const canEdit = isCustom && (S.role==='admin'||S.role==='superadmin');
  const canGen = S.role==='admin'||S.role==='superadmin';
  document.getElementById('detailPanel').innerHTML = `
    <div>
      <div class="section-title">YAML</div>
      <textarea class="yaml-area" id="yamlEditor" ${canEdit?'':'readonly'}>${esc(data.yaml_text)}</textarea>
      ${canEdit?`<div class="row-actions" style="margin-top:8px">
        <button class="btn btn-primary" onclick="saveParser('${esc(name)}')">Save</button>
        <button class="btn btn-secondary" onclick="deleteParser('${esc(name)}')">Delete</button>
      </div>`:''}
      <div class="msg" id="editMsg"></div>
    </div>
    <hr class="divider">
    <div>
      <div class="section-title">Test Parser</div>
      <input class="log-line-input" id="testInput" placeholder="Paste a log line…">
      <div class="row-actions" style="margin-top:8px">
        <button class="btn btn-secondary" onclick="testParser('${esc(name)}')">Test</button>
      </div>
      <div class="test-result" id="testResult" style="margin-top:8px">Results appear here</div>
    </div>
    ${canGen?`<hr class="divider">
    <div>
      <div class="section-title">AI Generator</div>
      <textarea class="yaml-area" id="genSample" style="min-height:80px" placeholder="Paste a raw log sample…"></textarea>
      <div class="row-actions" style="margin-top:8px">
        <button class="btn btn-secondary" id="genBtn" onclick="generateParser()">Generate with Claude</button>
      </div>
      <div id="genPreviewWrap" style="display:none;margin-top:12px">
        <div class="section-title">Preview (review before saving)</div>
        <textarea class="yaml-area" id="genPreview" style="min-height:160px"></textarea>
        <div class="row-actions" style="margin-top:8px">
          <button class="btn btn-primary" onclick="useGenerated()">Use this YAML</button>
        </div>
      </div>
      <div class="msg" id="genMsg"></div>
    </div>`:''}
  `;
  document.querySelectorAll('#parserList tr').forEach(tr=>{tr.classList.remove('selected')});
  document.querySelectorAll('#parserList tr').forEach(tr=>{
    if(tr.querySelector('td')?.textContent===name)tr.classList.add('selected');
  });
}

function newParser(){
  S.selected = null;
  document.getElementById('detailPanel').innerHTML = `
    <div>
      <div class="section-title">New Parser</div>
      <input class="log-line-input" id="newName" placeholder="parser-name (kebab-case)" style="margin-bottom:8px">
      <textarea class="yaml-area" id="yamlEditor" style="min-height:200px" placeholder="Paste or generate YAML…"></textarea>
      <div class="row-actions" style="margin-top:8px">
        <button class="btn btn-primary" onclick="createParser()">Create</button>
      </div>
      <div class="msg" id="editMsg"></div>
    </div>
    <hr class="divider">
    <div>
      <div class="section-title">AI Generator</div>
      <textarea class="yaml-area" id="genSample" style="min-height:80px" placeholder="Paste a raw log sample…"></textarea>
      <div class="row-actions" style="margin-top:8px">
        <button class="btn btn-secondary" id="genBtn" onclick="generateParser()">Generate with Claude</button>
      </div>
      <div id="genPreviewWrap" style="display:none;margin-top:12px">
        <div class="section-title">Preview</div>
        <textarea class="yaml-area" id="genPreview" style="min-height:160px"></textarea>
        <div class="row-actions" style="margin-top:8px">
          <button class="btn btn-primary" onclick="useGenerated()">Use this YAML</button>
        </div>
      </div>
      <div class="msg" id="genMsg"></div>
    </div>
  `;
}

async function createParser(){
  const name = document.getElementById('newName')?.value.trim();
  const yaml = document.getElementById('yamlEditor')?.value;
  const msg = document.getElementById('editMsg');
  if(!name||!yaml){showMsg(msg,'Name and YAML are required','err');return}
  const r = await api('/parsers',{method:'POST',body:JSON.stringify({name,yaml_text:yaml})});
  if(!r)return;
  const data = await r.json();
  if(r.ok){showMsg(msg,'Created','ok');await loadParsers();selectParser(name,true)}
  else showMsg(msg,data.detail||'Error','err');
}

async function saveParser(name){
  const yaml = document.getElementById('yamlEditor')?.value;
  const msg = document.getElementById('editMsg');
  const r = await api('/parsers/'+encodeURIComponent(name),{method:'PUT',body:JSON.stringify({name,yaml_text:yaml})});
  if(!r)return;
  const data = await r.json();
  if(r.ok)showMsg(msg,'Saved','ok');
  else showMsg(msg,data.detail||'Error','err');
}

async function deleteParser(name){
  if(!confirm('Delete parser "'+name+'"?'))return;
  const r = await api('/parsers/'+encodeURIComponent(name),{method:'DELETE'});
  if(!r)return;
  if(r.ok){S.selected=null;document.getElementById('detailPanel').innerHTML='<span>Select a parser to view or edit</span>';document.getElementById('detailPanel').className='detail-panel empty';await loadParsers()}
}

async function testParser(name){
  const line = document.getElementById('testInput')?.value;
  const result = document.getElementById('testResult');
  if(!line){result.textContent='Enter a log line above';return}
  result.textContent='Testing…';
  const r = await api('/parsers/'+encodeURIComponent(name)+'/test',{method:'POST',body:JSON.stringify({log_line:line})});
  if(!r)return;
  const data = await r.json();
  if(!r.ok){result.textContent='Error: '+(data.detail||r.status);return}
  if(!data.matched){result.textContent='No match';return}
  result.textContent = JSON.stringify(data.fields, null, 2);
}

async function generateParser(){
  const sample = document.getElementById('genSample')?.value;
  const btn = document.getElementById('genBtn');
  const msg = document.getElementById('genMsg');
  const wrap = document.getElementById('genPreviewWrap');
  if(!sample){showMsg(msg,'Paste a log sample first','err');return}
  btn.disabled=true;btn.textContent='Generating…';hideMsg(msg);
  const r = await api('/parsers/generate',{method:'POST',body:JSON.stringify({log_sample:sample})});
  btn.disabled=false;btn.textContent='Generate with Claude';
  if(!r)return;
  const data = await r.json();
  if(!r.ok){showMsg(msg,data.detail||'Error','err');return}
  document.getElementById('genPreview').value = data.yaml_text;
  wrap.style.display='block';
}

function useGenerated(){
  const yaml = document.getElementById('genPreview')?.value;
  const editor = document.getElementById('yamlEditor');
  if(editor)editor.value=yaml;
}

function showMsg(el,text,type){el.textContent=text;el.className='msg msg-'+(type==='ok'?'ok':'err');el.style.display='block';setTimeout(()=>{el.style.display='none'},3000)}
function hideMsg(el){el.style.display='none'}

function init(){
  TH.setAttribute('data-theme',localStorage.getItem('ts_theme')||'dark');
  const jwt = localStorage.getItem('ts_jwt');
  if(!jwt){window.location.href='/ui/login.html';return}
  const payload = parseJwt(jwt);
  if(!payload||payload.exp*1000<Date.now()){window.location.href='/ui/login.html';return}
  S.jwt = jwt;
  S.ep = localStorage.getItem('ts_ep')||'http://localhost:8000';
  S.role = payload.role||'';
  document.getElementById('navUser').textContent = payload.username||'';
  if(S.role==='admin'||S.role==='superadmin'){
    document.getElementById('newBtn').style.display='inline-flex';
  }
  loadParsers();
}
window.addEventListener('DOMContentLoaded',init);
</script>
</body>
</html>
```

- [ ] **Step 3: Create `ui/rules.html`**

```html
<!DOCTYPE html>
<html lang="en" data-theme="dark">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>TinySIEM — Rules</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:wght@400;500;600&family=IBM+Plex+Mono:wght@400;500&display=swap" rel="stylesheet">
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#0d0e17;--surface:#12131f;--surface2:#191a2d;--border:#252840;--border2:#323560;
  --text:#cdd0eb;--text-dim:#7b7fa8;--text-muted:#454870;
  --accent:#4d9fff;--accent-dim:#0d2a50;--err:#ef4444;--ok:#22c55e;
  --sev-low:#22c55e;--sev-medium:#f59e0b;--sev-high:#ef4444;--sev-critical:#a855f7;
  --sans:'IBM Plex Sans',sans-serif;--mono:'IBM Plex Mono',monospace;
}
[data-theme="light"]{
  --bg:#f0f2f8;--surface:#ffffff;--surface2:#f5f7fc;--border:#dde0f0;--border2:#c0c6e0;
  --text:#1a1c2e;--text-dim:#4a4f6a;--text-muted:#9096b8;--accent:#2979ff;--accent-dim:#e3ecff;
}
html,body{height:100%;background:var(--bg);color:var(--text);font-family:var(--sans);font-size:13px}
.layout{display:flex;height:100vh}
.sidebar{width:200px;flex-shrink:0;background:var(--surface);border-right:1px solid var(--border);display:flex;flex-direction:column;padding:16px 0}
.logo{display:flex;align-items:center;gap:8px;padding:0 16px 16px;font-size:15px;font-weight:600;border-bottom:1px solid var(--border);margin-bottom:8px}
.logo svg{color:var(--accent)}
.nav-item{display:block;padding:7px 16px;color:var(--text-dim);text-decoration:none;font-size:12px;font-weight:500;transition:background .1s,color .1s}
.nav-item:hover,.nav-item.active{background:var(--accent-dim);color:var(--accent)}
.sidebar-footer{margin-top:auto;padding:12px 16px;border-top:1px solid var(--border);font-size:11px;color:var(--text-muted)}
.main{flex:1;display:flex;flex-direction:column;overflow:hidden}
.topbar{padding:12px 20px;border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;background:var(--surface)}
.topbar h1{font-size:14px;font-weight:600}
.topbar-actions{display:flex;gap:8px;align-items:center}
.btn{padding:6px 12px;border-radius:4px;border:none;font-size:12px;font-weight:600;cursor:pointer;transition:opacity .15s}
.btn-primary{background:var(--accent);color:#fff}
.btn-secondary{background:var(--surface2);color:var(--text);border:1px solid var(--border2)}
.btn:hover{opacity:.85}
.btn:disabled{opacity:.45;cursor:not-allowed}
.content{flex:1;display:flex;overflow:hidden}
.list-panel{width:280px;flex-shrink:0;border-right:1px solid var(--border);overflow-y:auto}
.list-panel table{width:100%;border-collapse:collapse}
.list-panel th{padding:8px 12px;font-size:10px;font-weight:600;text-transform:uppercase;letter-spacing:.06em;color:var(--text-muted);border-bottom:1px solid var(--border);background:var(--surface);position:sticky;top:0}
.list-panel td{padding:8px 12px;border-bottom:1px solid var(--border);cursor:pointer}
tr.selected td,.list-panel tr:hover td{background:var(--accent-dim)}
.sev{display:inline-block;padding:2px 6px;border-radius:3px;font-size:10px;font-weight:700;text-transform:uppercase}
.sev-low{color:var(--sev-low)}
.sev-medium{color:var(--sev-medium)}
.sev-high{color:var(--sev-high)}
.sev-critical{color:var(--sev-critical)}
.badge{display:inline-block;padding:2px 6px;border-radius:3px;font-size:10px;font-weight:600;background:var(--surface2);color:var(--text-dim)}
.badge-custom{background:#1a2a1a;color:var(--ok)}
.detail-panel{flex:1;overflow-y:auto;padding:20px;display:flex;flex-direction:column;gap:16px}
.detail-panel.empty{align-items:center;justify-content:center;color:var(--text-muted)}
.section-title{font-size:11px;font-weight:600;text-transform:uppercase;letter-spacing:.06em;color:var(--text-muted);margin-bottom:8px}
.yaml-area{width:100%;min-height:200px;background:var(--surface2);border:1px solid var(--border2);border-radius:4px;padding:10px;color:var(--text);font-family:var(--mono);font-size:12px;line-height:1.5;resize:vertical;outline:none}
.yaml-area:focus{border-color:var(--accent)}
.yaml-area[readonly]{opacity:.7;cursor:default}
.row-actions{display:flex;gap:8px}
.msg{font-size:12px;padding:6px 10px;border-radius:4px;display:none;margin-top:8px}
.msg-ok{background:#0d2a0d;color:var(--ok)}
.msg-err{background:#2a0d0d;color:var(--err)}
.divider{border:none;border-top:1px solid var(--border);margin:4px 0}
.field-input{width:100%;background:var(--surface2);border:1px solid var(--border2);border-radius:4px;padding:7px 10px;color:var(--text);font-family:var(--sans);font-size:12px;outline:none;margin-bottom:8px}
.field-input:focus{border-color:var(--accent)}
.theme-btn{background:none;border:1px solid var(--border);border-radius:4px;padding:4px 8px;color:var(--text-dim);cursor:pointer;font-size:13px}
</style>
</head>
<body>
<div class="layout">
  <aside class="sidebar">
    <div class="logo">
      <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
      TinySIEM
    </div>
    <a class="nav-item" href="/ui/dashboard.html">Dashboard</a>
    <a class="nav-item" href="/ui/events.html">Events</a>
    <a class="nav-item" href="/ui/alerts.html">Alerts</a>
    <a class="nav-item" href="/ui/parsers.html">Parsers</a>
    <a class="nav-item active" href="/ui/rules.html">Rules</a>
    <a class="nav-item" href="/ui/configuration.html">Configuration</a>
    <div class="sidebar-footer">
      <div id="navUser"></div>
      <a href="#" onclick="logout()" style="color:var(--text-muted);text-decoration:none;font-size:11px">Sign out</a>
    </div>
  </aside>
  <div class="main">
    <div class="topbar">
      <h1>Rules</h1>
      <div class="topbar-actions">
        <button class="theme-btn" onclick="toggleTheme()">◑</button>
        <button class="btn btn-primary" id="newBtn" onclick="newRule()" style="display:none">+ New Rule</button>
      </div>
    </div>
    <div class="content">
      <div class="list-panel">
        <table>
          <thead><tr><th>Name</th><th>Sev</th><th>Source</th></tr></thead>
          <tbody id="ruleList"></tbody>
        </table>
      </div>
      <div class="detail-panel empty" id="detailPanel">
        <span>Select a rule to view or edit</span>
      </div>
    </div>
  </div>
</div>
<script>
const TH = document.documentElement;
const S = {ep:'', jwt:'', role:'', selected:null};

function toggleTheme(){const t=TH.getAttribute('data-theme')==='dark'?'light':'dark';TH.setAttribute('data-theme',t);localStorage.setItem('ts_theme',t)}
function parseJwt(t){try{const b=t.split('.')[1].replace(/-/g,'+').replace(/_/g,'/');return JSON.parse(atob(b))}catch{return null}}
function logout(){localStorage.removeItem('ts_jwt');localStorage.removeItem('ts_username');localStorage.removeItem('ts_role');window.location.href='/ui/login.html'}
function esc(s){return String(s??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;')}
function sevClass(s){return 'sev sev-'+(s||'low').toLowerCase()}

async function api(path,opts={}){
  const res = await fetch(S.ep+path,{headers:{'Authorization':'Bearer '+S.jwt,'Content-Type':'application/json'},...opts});
  if(res.status===401){logout();return null}
  return res;
}

async function loadRules(){
  const r = await api('/rules');
  if(!r)return;
  const data = await r.json();
  const tbody = document.getElementById('ruleList');
  tbody.innerHTML = data.rules.map(rule=>`
    <tr onclick="selectRule('${esc(rule.name)}',${rule.is_custom})">
      <td>${esc(rule.name)}</td>
      <td><span class="${sevClass(rule.severity)}">${esc(rule.severity)}</span></td>
      <td>${esc(rule.source)}</td>
    </tr>`).join('');
}

async function selectRule(name, isCustom){
  S.selected = name;
  const r = await api('/rules/'+encodeURIComponent(name));
  if(!r)return;
  const data = await r.json();
  const canEdit = isCustom && (S.role==='admin'||S.role==='superadmin');
  const canGen = S.role==='admin'||S.role==='superadmin';
  document.getElementById('detailPanel').className = 'detail-panel';
  document.getElementById('detailPanel').innerHTML = `
    <div>
      <div class="section-title">YAML</div>
      <textarea class="yaml-area" id="yamlEditor" ${canEdit?'':'readonly'}>${esc(data.yaml_text)}</textarea>
      ${canEdit?`<div class="row-actions" style="margin-top:8px">
        <button class="btn btn-primary" onclick="saveRule('${esc(name)}')">Save</button>
        <button class="btn btn-secondary" onclick="deleteRule('${esc(name)}')">Delete</button>
      </div>`:''}
      <div class="msg" id="editMsg"></div>
    </div>
    ${canGen?`<hr class="divider">
    <div>
      <div class="section-title">AI Generator</div>
      <input class="field-input" id="genSource" placeholder="Source (e.g. nginx)">
      <textarea class="yaml-area" id="genDesc" style="min-height:80px" placeholder="Describe the detection in plain English…"></textarea>
      <div class="row-actions" style="margin-top:8px">
        <button class="btn btn-secondary" id="genBtn" onclick="generateRule()">Generate with Claude</button>
      </div>
      <div id="genPreviewWrap" style="display:none;margin-top:12px">
        <div class="section-title">Preview (review before saving)</div>
        <textarea class="yaml-area" id="genPreview" style="min-height:160px"></textarea>
        <div class="row-actions" style="margin-top:8px">
          <button class="btn btn-primary" onclick="useGenerated()">Use this YAML</button>
        </div>
      </div>
      <div class="msg" id="genMsg"></div>
    </div>`:''}
  `;
}

function newRule(){
  S.selected = null;
  document.getElementById('detailPanel').className = 'detail-panel';
  document.getElementById('detailPanel').innerHTML = `
    <div>
      <div class="section-title">New Rule</div>
      <input class="field-input" id="newName" placeholder="rule-name (kebab-case)">
      <textarea class="yaml-area" id="yamlEditor" style="min-height:200px" placeholder="Paste or generate YAML…"></textarea>
      <div class="row-actions" style="margin-top:8px">
        <button class="btn btn-primary" onclick="createRule()">Create</button>
      </div>
      <div class="msg" id="editMsg"></div>
    </div>
    <hr class="divider">
    <div>
      <div class="section-title">AI Generator</div>
      <input class="field-input" id="genSource" placeholder="Source (e.g. nginx)">
      <textarea class="yaml-area" id="genDesc" style="min-height:80px" placeholder="Describe the detection…"></textarea>
      <div class="row-actions" style="margin-top:8px">
        <button class="btn btn-secondary" id="genBtn" onclick="generateRule()">Generate with Claude</button>
      </div>
      <div id="genPreviewWrap" style="display:none;margin-top:12px">
        <div class="section-title">Preview</div>
        <textarea class="yaml-area" id="genPreview" style="min-height:160px"></textarea>
        <div class="row-actions" style="margin-top:8px">
          <button class="btn btn-primary" onclick="useGenerated()">Use this YAML</button>
        </div>
      </div>
      <div class="msg" id="genMsg"></div>
    </div>
  `;
}

async function createRule(){
  const name = document.getElementById('newName')?.value.trim();
  const yaml = document.getElementById('yamlEditor')?.value;
  const msg = document.getElementById('editMsg');
  if(!name||!yaml){showMsg(msg,'Name and YAML required','err');return}
  const r = await api('/rules',{method:'POST',body:JSON.stringify({name,yaml_text:yaml})});
  if(!r)return;
  const data = await r.json();
  if(r.ok){showMsg(msg,'Created','ok');await loadRules();selectRule(name,true)}
  else showMsg(msg,data.detail||'Error','err');
}

async function saveRule(name){
  const yaml = document.getElementById('yamlEditor')?.value;
  const msg = document.getElementById('editMsg');
  const r = await api('/rules/'+encodeURIComponent(name),{method:'PUT',body:JSON.stringify({name,yaml_text:yaml})});
  if(!r)return;
  const data = await r.json();
  if(r.ok)showMsg(msg,'Saved','ok');
  else showMsg(msg,data.detail||'Error','err');
}

async function deleteRule(name){
  if(!confirm('Delete rule "'+name+'"?'))return;
  const r = await api('/rules/'+encodeURIComponent(name),{method:'DELETE'});
  if(!r)return;
  if(r.ok){S.selected=null;document.getElementById('detailPanel').innerHTML='<span>Select a rule to view or edit</span>';document.getElementById('detailPanel').className='detail-panel empty';await loadRules()}
}

async function generateRule(){
  const src = document.getElementById('genSource')?.value.trim();
  const desc = document.getElementById('genDesc')?.value.trim();
  const btn = document.getElementById('genBtn');
  const msg = document.getElementById('genMsg');
  const wrap = document.getElementById('genPreviewWrap');
  if(!src||!desc){showMsg(msg,'Source and description required','err');return}
  btn.disabled=true;btn.textContent='Generating…';hideMsg(msg);
  const r = await api('/rules/generate',{method:'POST',body:JSON.stringify({description:desc,source:src})});
  btn.disabled=false;btn.textContent='Generate with Claude';
  if(!r)return;
  const data = await r.json();
  if(!r.ok){showMsg(msg,data.detail||'Error','err');return}
  document.getElementById('genPreview').value = data.yaml_text;
  wrap.style.display='block';
}

function useGenerated(){
  const yaml = document.getElementById('genPreview')?.value;
  const editor = document.getElementById('yamlEditor');
  if(editor)editor.value=yaml;
}

function showMsg(el,text,type){el.textContent=text;el.className='msg msg-'+(type==='ok'?'ok':'err');el.style.display='block';setTimeout(()=>{el.style.display='none'},3000)}
function hideMsg(el){el.style.display='none'}

function init(){
  TH.setAttribute('data-theme',localStorage.getItem('ts_theme')||'dark');
  const jwt = localStorage.getItem('ts_jwt');
  if(!jwt){window.location.href='/ui/login.html';return}
  const payload = parseJwt(jwt);
  if(!payload||payload.exp*1000<Date.now()){window.location.href='/ui/login.html';return}
  S.jwt = jwt;
  S.ep = localStorage.getItem('ts_ep')||'http://localhost:8000';
  S.role = payload.role||'';
  document.getElementById('navUser').textContent = payload.username||'';
  if(S.role==='admin'||S.role==='superadmin'){
    document.getElementById('newBtn').style.display='inline-flex';
  }
  loadRules();
}
window.addEventListener('DOMContentLoaded',init);
</script>
</body>
</html>
```

- [ ] **Step 4: Verify all tests pass**

```bash
pytest app/tests/ -v
```

Expected: all tests pass (UI changes don't affect backend tests).

- [ ] **Step 5: Commit**

```bash
git add ui/parsers.html ui/rules.html ui/dashboard.html ui/events.html ui/alerts.html ui/users.html ui/configuration.html
git commit -m "feat: parsers and rules UI pages with AI generator and test panel"
```

---

## Self-Review Checklist (run before handoff)

- [ ] `pytest app/tests/ -v` — all pass
- [ ] `docker-compose up --build` — container starts, `curl http://localhost:8000/health` returns `"0.6.0"`
- [ ] `GET /parsers` returns `nginx-access` (not `nginx_access`)
- [ ] `GET /rules` returns `nginx-http-404-spike` and `nginx-http-500-error`
- [ ] `POST /parsers` with analyst JWT returns 403
- [ ] `POST /parsers/generate` with no API key returns 503
- [ ] `POST /parsers/nginx-access/test` with valid nginx log returns `matched: true`
- [ ] `/ui/parsers.html` and `/ui/rules.html` load correctly and nav links work
- [ ] `PUT /parsers/nginx-access` returns 403 (built-in is read-only)
- [ ] Name with `../` in path param returns 422
